import React from "react";
import PropTypes from "prop-types";
import GameLayout from "./GameLayout";

const Game = (props) => {
	return <GameLayout />;
};

Game.propTypes = {};

export default Game;
